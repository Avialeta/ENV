[Unit]
Description=Avialeta: Cache

[Service]
ExecStartPre=-/usr/bin/docker rm -v avialeta-cache
ExecStart=/usr/bin/docker run \
	--name avialeta-cache \
	redis:latest
ExecStop=/usr/bin/docker stop avialeta-cache

[Install]
WantedBy=default.target
